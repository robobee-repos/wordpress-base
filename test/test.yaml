version: '3.1'

services:

  wordpress:
    image: erwin82/wordpress:latest
    environment:
      WORDPRESS_DB_HOST: mysql:3306
      WORDPRESS_DB_PASSWORD: example
    depends_on:
      - mysql
    links:
      - mysql
    volumes:
      - ${ROOT_DIR}:/var/www/html

  mysql:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: example

  nginx:
    image: erwin82/nginx:latest
    ports:
      - 8080:8080
    depends_on:
      - wordpress
    links:
      - wordpress
    volumes:
      - ${NGINX_CONF_DIR}:/nginx-in
      - ${ROOT_DIR}:/var/www/html
